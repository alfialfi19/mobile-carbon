name: mobile_carbon
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  cached_network_image: ^3.1.0+1
  crypto: ^3.0.1 # For hashing algorithms
  cupertino_icons: ^1.0.4
  dio: ^4.0.4
  dotted_decoration: ^2.0.0
  easy_localization: ^3.0.0   # Internationalizing and localization
  equatable: ^2.0.3
  file_picker: ^4.2.4
  flutter:
    sdk: flutter
  flutter_bloc: ^7.2.1
  flutter_html: ^2.1.5 # Html Decoder to Text widget library
  flutter_secure_storage: "5.0.2"
  flutter_svg: ^0.23.0+1
  fluttertoast: ^8.0.8
  formz: ^0.4.1
  get_it: ^7.2.0
  heic_to_jpg: ^0.2.0
  http: ^0.13.5
  image_picker: ^0.8.6
  japx: ^2.0.3
  json_annotation: ^4.3.0   # Annotation for json serialization and deserialization
  #  requires SDK version >=2.17.1 <3.0.0
  #  pdfx: ^2.2.0  # PDF viewer and renderer using native API
  path_provider: ^2.0.11
  shimmer: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
